
name: Package Application with Pyinstaller

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python 3.13.5
      uses: actions/setup-python@v5
      with:
        python-version: "3.13.5"
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller # 安装打包工具
        
    - name: Package Application
      run: pyinstaller --onefile --windowed --name "EpubMerger" main.py --hidden-import PyQt5.sip --hidden-import PyQt5.QtCore --hidden-import PyQt5.QtGui --hidden-import PyQt5.QtWidgets --clean
      
    - uses: actions/upload-artifact@v4
      with:
        name: windows-build
        path: dist/EpubMerger.exe
